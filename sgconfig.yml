# ast-grep configuration for tree-sitter-pike
# Usage: build the parser first with `tree-sitter build --output pike.so`
ruleDirs: [rules]
customLanguages:
  pike:
    libraryPath: pike.so
    extensions: [pike, pmod]
    expandoChar: _
